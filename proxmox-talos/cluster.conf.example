# Proxmox Configuration
proxmox_api_url           = "https://10.10.21.31:8006/api2/json"
proxmox_api_token_id      = "terraform@pve!terraform-token"
proxmox_api_token_secret  = "0b62f0b-e403-4697-be3b-9ca62a4df295"
proxmox_tls_insecure      = true
proxmox_node              = "firefly"

# Storage and Network
storage_pool    = "local-lvm"  # Update with your storage pool name
network_bridge  = "vmbr0"      # Update with your network bridge name
iso_storage     = "local"      # Storage where the Talos ISO is uploaded

# Talos Cluster Configuration
cluster_name        = "laternfly"     # Name of your Kubernetes cluster
control_plane_ip    = "10.10.21.110"
worker_node_01_ip   = "10.10.21.111"
worker_node_02_ip   = "10.10.21.112"

# MAC Address Configuration
# Format: xx:xx:xx:xx:xx:xx or xx-xx-xx-xx-xx-xx
# These MAC addresses should be unique within your network
control_plane_mac   = "bc:24:11:82:9f:fb"
worker_01_mac       = "bc:24:11:51:6f:4d"
worker_02_mac       = "bc:24:11:82:9f:3c"

# ISO Configuration
talos_iso = "talos-v1.11.1-amd64.iso"

# GitOps Configuration
# Git repository for ArgoCD to sync applications from
git_repo_url = "https://github.com/fafiorim/homelab"
git_repo_branch = "main"  # or "master" depending on your default branch