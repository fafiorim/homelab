apiVersion: v1
kind: PersistentVolume
metadata:
  name: chat-hub-data-pv
  labels:
    app: chat-hub
    volume: data
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: nfs
  nfs:
    server: 10.10.21.11
    path: /volume4/VM/containers/chat-hub/data
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: chat-hub-data-pvc
  namespace: chat-hub
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs
  resources:
    requests:
      storage: 5Gi
  selector:
    matchLabels:
      app: chat-hub
      volume: data
