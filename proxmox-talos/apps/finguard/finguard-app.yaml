apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: finguard
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/fafiorim/homelab
    targetRevision: main
    path: proxmox-talos/apps/finguard
  destination:
    server: https://kubernetes.default.svc
    namespace: finguard
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
